---
title: Assets
date: '2026-06-13T00:00:00.000Z'
description: How images and binary assets are stored, referenced, and tracked through renames and moves.
id: 2njQMsf8fvOHHwDFytmV73
version: 1
---

<h1>Assets</h1><h2>Storage convention</h2><p>Every HD document with binary assets has its own folder under a centralized <code>.hd/</code> directory at the workspace root. The folder is named with the document's base62 id — only the id, with no document-path mirroring:</p><pre><code>workspace/
├── docs/
│   └── architecture/
│       └── overview.hd     ← id: 7gXk2L9pM4nQ8vR1cT3bDe
└── .hd/
    └── 7gXk2L9pM4nQ8vR1cT3bDe/
        ├── image-1.png
        └── diagram.svg</code></pre><p>The id is the entire lookup key. Renaming or moving the <code>.hd</code> file — inside the editor, via the file explorer, via git, via any external tool — has no effect on the asset folder's location.</p><h2>How assets are referenced inside a document</h2><p>Inside the <code>.hd</code> file, assets are referenced by bare filename:</p><pre><code>&lt;img src="image-1.png" alt="Architecture overview"&gt;</code></pre><p>The editor and any future serve-time renderer resolve bare filenames against the document's asset folder. On save, image <code>src</code> attributes are normalized back to bare filenames so the file stays portable and the on-disk source stays clean.</p><h2>What happens on common operations</h2><table style="min-width: 50px;"><colgroup><col style="min-width: 25px;"><col style="min-width: 25px;"></colgroup><tbody><tr><th colspan="1" rowspan="1"><p>Operation</p></th><th colspan="1" rowspan="1"><p>What the editor does</p></th></tr><tr><td colspan="1" rowspan="1"><p>Paste an image (clipboard or drag-drop)</p></td><td colspan="1" rowspan="1"><p>Saves the bytes to the document's asset folder as <code>image-N.&lt;ext&gt;</code>; inserts an <code>&lt;img&gt;</code> tag with the bare filename at the cursor.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Rename a <code>.hd</code> file</p></td><td colspan="1" rowspan="1"><p>Nothing happens on disk for assets. The asset folder is keyed by <code>id</code>, not by document path.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Move a <code>.hd</code> file to a different directory</p></td><td colspan="1" rowspan="1"><p>Nothing happens on disk for assets. Same reason — the path is not part of the lookup.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Duplicate a <code>.hd</code> file via the file explorer</p></td><td colspan="1" rowspan="1"><p>The copy gets a fresh <code>id</code> on first open in the editor; a new (empty) asset folder is created the first time you add an asset. The original's assets stay with the original.</p></td></tr><tr><td colspan="1" rowspan="1"><p>Delete a <code>.hd</code> file outside the editor</p></td><td colspan="1" rowspan="1"><p>The asset folder is orphaned. A "garbage-collect orphaned assets" command is planned.</p></td></tr></tbody></table><h2>Why folders use the id instead of the filename</h2><p>If asset folders were named after the document file (e.g., <code>.hd/docs/overview/</code>), renaming the document would either break asset references or require coordinated folder renames. Keying the folder by the document's <code>id</code> decouples the asset location from the filename: renames, moves, and external file operations don't touch the asset folder at all.</p><h2>Migrating from the legacy mirrored layout</h2><p>An earlier version of HD mirrored the document's directory path into <code>.hd/</code>, producing folders like <code>.hd/docs/architecture/&lt;id&gt;/</code>. The current layout drops that mirroring — only the id remains.</p><p>If your workspace still has the mirrored layout, the editor will continue to render assets (it transparently falls back to the legacy path when the flat path doesn't exist on disk). To migrate explicitly:</p><ul><li><p><strong>Inside VS Code</strong>: open the command palette (<strong>Ctrl+Shift+P</strong>) and run <code>HD: Migrate Workspace Assets to Flat Layout</code>. It scans the workspace, shows a plan with counts and per-id details in the "HD Migrate" output channel, and asks for confirmation before moving anything. Already-flat folders are left untouched.</p></li><li><p><strong>From the command line</strong> (any workspace, no VS Code required): <code>node plugin/hd/scripts/migrate-assets-flat.mjs &lt;workspace&gt;</code> for a dry run, then re-run with <code>--apply</code> to perform moves.</p></li></ul><p>Both the command and the script handle the same edge cases: id collisions (flat and mirrored both present for the same id) are reported and left alone; orphan id folders with no matching document are reported and left alone; documents missing the <code>id</code> frontmatter are skipped. The fallback that lets un-migrated workspaces still render assets will be removed in a future release once the migration is complete across known workspaces.</p><h2>Cross-document asset references</h2><p>The base implementation expects each document to own its assets. Cross-document references (Document A linking to an image in Document B's asset folder) are not part of the current spec; if shared assets become a recurring need, a <code>.hd/_shared/</code> convention is the likely answer.</p>